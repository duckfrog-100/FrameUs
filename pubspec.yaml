name: frameus
description: "FrameUs - same-composition emotional records for friends and couples."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4

flutter:
  uses-material-design: true
